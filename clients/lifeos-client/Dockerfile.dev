# ============================================
# LifeOS Client - Development Dockerfile
# Hot Reload için Vite Dev Server kullanır
# ============================================
FROM node:20-alpine

# Healthcheck için wget ekle
RUN apk add --no-cache wget

WORKDIR /app

# Install dependencies (cache için önce package dosyalarını kopyala)
COPY package.json package-lock.json* ./
RUN npm ci

# Source code volume mount ile bağlanacak, bu yüzden burada kopyalamıyoruz
# Sadece node_modules ve package.json hazır olacak

# Vite dev server port
EXPOSE 5173

# Development server'ı başlat
CMD ["npm", "run", "dev"]

